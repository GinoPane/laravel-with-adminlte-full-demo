<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        @yield('backend.main.header.content.title', 'Page Header here')
        <small>@yield('backend.main.header.content.description')</small>
    </h1>
    <ol class="breadcrumb">
        @yield('backend.main.header.content.breadcrumb')
    </ol>
</section>